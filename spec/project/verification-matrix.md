# GenOffice Project — Verification Matrix

| Work item        | Semantic evidence                                  | Static/architecture evidence                      | Host evidence                         | File evidence                   | Completion gate                                                         |
| ---------------- | -------------------------------------------------- | ------------------------------------------------- | ------------------------------------- | ------------------------------- | ----------------------------------------------------------------------- |
| PROJECT-001      | contract compile                                   | package boundary scan; lock review                | N/A                                   | N/A                             | six specs committed and internally consistent                           |
| PROJECT-002      | domain validation tests                            | stable-ID architecture tests                      | N/A                                   | schema serialization fixture    | invalid identities/references rejected                                  |
| PROJECT-003      | command/journal tests                              | forbidden raw-mutation scan                       | N/A                                   | N/A                             | semantic command union + history invariants pass                        |
| PROJECT-004      | calendar golden suite                              | host-import scan                                  | N/A                                   | calendar fixture                | repeated working-time calculations match                                |
| PROJECT-005      | graph validation/topology suite                    | dependency determinism scan                       | N/A                                   | graph fixture                   | all link types + invalid graph rejection pass                           |
| PROJECT-006      | scheduling golden suite + repeat-run byte equality | no UI/host imports; deterministic iteration guard | N/A                                   | golden ProjectDocument fixtures | dates, dependencies, critical path, total/free float all match expected |
| PROJECT-007..013 | domain/scheduling goldens                          | package boundaries                                | later E2E                             | later save/reopen               | semantic acceptance before renderer work                                |
| PROJECT-014..020 | round-trip/import/export goldens                   | parser isolation checks                           | N/A                                   | golden files + diagnostics      | no silent loss                                                          |
| PROJECT-021..031 | command-to-renderer integration                    | renderer dependency checks                        | desktop + Playwright E2E              | save/reopen                     | semantic and renderer parity                                            |
| PROJECT-032..045 | view/scheduling/resource tests                     | view projections have no scheduling authority     | desktop/web E2E                       | view fixtures                   | each view matches canonical derived state                               |
| PROJECT-046      | cross-host scenario suite                          | package boundary scan                             | desktop/web parity                    | persisted scenario artifacts    | identical semantic outcomes                                             |
| PROJECT-047      | golden compatibility suite                         | adapter isolation                                 | optional host smoke                   | golden file corpus              | import/export regressions blocked                                       |
| PROJECT-048      | benchmark suite                                    | performance architecture assertions               | desktop/web performance runs          | representative project fixtures | agreed large-project budgets met                                        |
| PROJECT-049      | production E2E                                     | release architecture gate                         | desktop + web production verification | save/reopen + import/export     | release acceptance complete                                             |

## Foundation evidence requirements

PROJECT-006 must include at minimum:

- sequential dependency fixture
- FS/SS/FF/SF fixture
- lag fixture
- holiday/calendar fixture
- critical-path/float fixture
- cycle/self/missing-reference rejection tests
- repeated scheduling equality test

Agent narrative is never sufficient evidence without these artifacts.

## PROJECT-012 evidence requirements

PROJECT-012 (Critical path / float edge cases) must include at minimum:

- 18 golden fixtures CP01–CP18 covering: simple critical chain, two/three critical paths, diamond convergence, mixed dependency types, lag/lead, free-vs-total slack, negative slack, near-critical branch, critical/noncritical milestone, summary criticality, nested summary, constraint + critical path, calendar boundary, holiday, isolated branch, reordered deterministic input.
- additional required tests covering: fan-out, fan-in, FS lag, FS lead, SS lag, FF lag, SF lag, multiple successors, freeSlack < totalSlack, freeSlack = totalSlack, zero free slack, negative slack, one-day near-critical, one-hour near-critical, critical milestone, noncritical milestone, milestone with predecessor, summary critical roll-up, nested summary critical path, SNET/MSO/FNET/MFO constraint interaction, different task calendars, weekend boundary, holiday boundary, isolated independent task, repeated deterministic schedule, serialized round-trip byte equality.
- every applicable golden fixture asserts exact `taskId`, `earlyStart`, `earlyFinish`, `lateStart`, `lateFinish`, `totalSlack`, `freeSlack`, `critical`, `scheduledStart`, `scheduledFinish`, and `projectFinish` — not merely counts of critical tasks.
- byte-identical `DerivedSchedule` output under repeated runs, reversed task/dependency arrays, and reordered parallel branches.
- all accepted PROJECT-006 through PROJECT-011 tests remain green (no regression, no weakened goldens).

## PROJECT-013 evidence requirements

PROJECT-013 (Resource leveling) must include at minimum:

- 29 golden fixtures L01–L29 covering: simple two-task overload, three-task overload, 200% single-assignment demand, priority resolution, TaskId tie-break (equal priority), critical-task policy (respectCritical true and false), FS dependency propagation, SS dependency propagation, FF dependency propagation, SF dependency propagation, positive lag, negative lead, SNET protection, MSO protection, MFO protection, deadline interaction (respectDeadlines true and false), resource calendar restriction (L17 — conflict detected on a resource-WORKING day; delayed start advances past a non-working day to the next resource-working instant), availability window restriction, multiple resources on the same task, baseline preservation, impossible leveling (both MSO), repeated deterministic leveling, reordered input leveling (reversed task/assignment/resource arrays + serialized round-trip), material/cost resources excluded, mid-assignment capacity drop (L25 — over-allocation bounded only by availability-window transitions; no assignment event brackets the conflict), resource unavailable no false over-allocation (L26 — two task-calendar windows overlapping on a non-working resource day produce NO over-allocation), partial calendar overlap conflict only in the intersection (L27 — conflict window clipped to the resource-working intersection of the assignment), task-calendar overlap removed by resource calendar (L28 — two tasks whose task-calendar windows overlap but whose resource-working demand periods are disjoint produce NO over-allocation), resource calendar + availability window simultaneously (L29 — the conflict window is clipped at the resource working boundary even when an availability window extends past it).
- additional required tests covering: SNLT protection, FNET protection, FNLT protection, milestone behavior (zero-duration skipped), summary behavior (not directly levelable; rolls up), manual task behavior (protected; LEVELING_PROTECTED_MANUAL when both sides manual), already-non-overallocated resource (LEVELING_NO_OVERALLOCATION), leveling result reapplied through the canonical scheduler (individual SetTaskStart dispatch matches the LevelResources batch), stable-ID preservation (TaskId/ResourceId/AssignmentId/DependencyId/BaselineId unchanged), no new dependency cycles, scope filter (taskIds), scope-empty (LEVELING_SCOPE_EMPTY), different assignment units, one 200% assignment (LEVELING_INCOMPLETE), FS predecessor-delay propagation to successor, split-day task calendar, LevelingResult shape, schedule-after-leveling is canonical, multi-window conflict identity (the same assignments producing two distinct conflict windows are reported as two distinct over-allocations — they do NOT collapse into one entry whose `resolved` state the final pass would overwrite).
- every applicable golden fixture asserts exact `taskId`, `scheduledStart`, `scheduledFinish`, `proposedCommands` (the semantic `SetTaskStart` values), `affectedTaskIds`, `overallocations.peakDemand`, `overallocations.maxUnits`, `overallocations.resolved`, and (where applicable) `critical`, `totalSlack`, `deadline`, `deadlineMissed`, `deadlineVariance`, `constraintType`, `constraintDate`.
- byte-identical `LevelingResult` output (proposedCommands + actions + overallocations + diagnostics) under three repeated runs, reversed task arrays, reversed assignment arrays, reversed resource arrays, and serialized round-trip (JSON parse).
- idempotency: leveling an already-leveled document emits `LEVELING_NO_OVERALLOCATION` with empty `proposedCommands`.
- semantic-command verification: applying `proposedCommands` via individual `SetTaskStart` dispatch produces the same document + schedule as the `LevelResources` batch.
- impossible-leveling verification: when no task can be delayed (both MSO, or all manual), `proposedCommands` is empty, the document is unchanged, and the diagnostic (`LEVELING_CONSTRAINT_CONFLICT` / `LEVELING_PROTECTED_MANUAL` / `LEVELING_NO_ELIGIBLE_TASK`) is surfaced.
- baseline immutability: baseline snapshots (`start`, `finish`, `duration`, `work`, `cost`), baseline IDs, and `capturedAt` are unchanged after leveling; only the current schedule's `task.start` candidates move.
- identity preservation: `TaskId`, `ResourceId`, `AssignmentId`, `DependencyId`, and `BaselineId` sets are unchanged; the dependency graph remains acyclic (the leveled document still schedules cleanly).
- no-regression: all accepted PROJECT-006 through PROJECT-012 tests remain green (no weakened goldens, no changed CPM formulas).
- sweep segmentation: the over-allocation sweep includes availability-window boundaries (start/finish) AND resource-calendar working-period boundaries (start/finish) in addition to assignment start/finish endpoints. A golden regression (L25) proves an over-allocation that arises ONLY from a mid-assignment capacity drop (no assignment event brackets the conflict) is detected and resolved.
- resource-calendar-aware demand: over-allocation is evaluated only where the resource can actually supply work capacity (its resolved working periods). During non-working segments demand is zero, so conflict windows are clipped to the resource-working intersection of the assignment and two task-calendar windows overlapping on a non-working resource day produce NO false over-allocation. Golden regressions L26 (no false over-allocation on a non-working resource day), L27 (conflict clipped to the partial resource-working intersection), L28 (task-calendar overlap removed by disjoint resource-working demand periods), and L29 (conflict clipped at the resource working boundary even when an availability window extends past it) prove the semantics. All four are genuine regressions (they fail against the pre-fix code that ignored the resource calendar in detection).
- shared task-start mutation kernel: the `LevelResources` batch applies each proposed `SetTaskStart` through the SAME canonical mutation primitive (`mutateForSetTaskStart`) used by the individual `SetTaskStart` command path, so there is exactly ONE authoritative task-start mutation implementation in the engine (task-existence validation, date parsing, the `withOptionalDate` field update, and inverse computation are all shared). `LevelResources` cannot develop semantics different from `SetTaskStart`.
- conflict-signature window identity: the conflict signature includes the conflict window start + finish (epoch-normalized). A multi-window regression proves the same assignments producing two distinct conflict windows are reported as two distinct over-allocations (no collapse, no `resolved`-state overwrite by the final pass).
- material/cost exclusion asserted exactly: no `|| true` tautology; the over-allocations list is asserted to contain only work-resource ids, and the exact resource-id set is asserted.

## PROJECT-014 evidence requirements

PROJECT-014 (Native `.gproj` format) must include at minimum:

- 15 golden fixtures G01–G15 covering: G01 minimal project, G02 WBS hierarchy, G03 dependency graph, G04 calendar-rich (inheritance + exception), G05 resources + assignments, G06 baseline-rich, G07 constraints/deadlines/progress, G08 work/cost, G09 custom fields, G10 views/tables/filters/groups, G11 multi-baseline, G12 large project (60 tasks, 50 dependencies, 10 resources, 60 assignments), G13 invalid schema, G14 unsupported version, G15 malformed references.
- 30 required tests covering: (1) minimal round-trip, (2) multiple tasks round-trip, (3) WBS hierarchy round-trip, (4) dependencies round-trip, (5) all FS/SS/FF/SF dependencies, (6) calendars round-trip, (7) calendar inheritance round-trip, (8) calendar exceptions round-trip, (9) resources round-trip, (10) assignments round-trip, (11) baselines round-trip, (12) multiple baselines, (13) constraints round-trip, (14) deadlines round-trip, (15) progress round-trip, (16) work/cost round-trip, (17) custom fields round-trip, (18) views/tables/filters/groups round-trip, (19) invalid file rejection, (20) unsupported version rejection, (21) missing required field rejection, (22) invalid reference rejection, (23) malformed calendar rejection, (24) malformed baseline rejection, (25) malformed assignment rejection, (26) deterministic serialization, (27) deterministic deserialization, (28) serialize→deserialize→serialize byte identity, (29) reordered-input deterministic serialization, (30) large project serialization.
- every valid golden fixture asserts: zero error-level diagnostics on round-trip; `serialize(deserialize(serialize(doc)))` byte-identical to `serialize(doc)`; semantic identity (`serialize(deserialize(serialize(doc)))` byte-identical to `serialize(build())`).
- every invalid golden fixture asserts: an error-level diagnostic with the expected code (`INVALID_TASK` / `UNSUPPORTED_GPROJ_VERSION` / `MISSING_TASK_REFERENCE`); the document is the empty/partial recovery form.
- byte-identical output under repeated runs, reversed-task-array builds (via counter-reset builders), and serialized round-trips.
- the serialized envelope NEVER contains derived-schedule keys (`earlyStart`, `totalSlack`, `critical`, `scheduledStart`, `taskSchedules`, `assignmentSchedules`, `deadlineVariance`, `LevelingResult`) — derived state is excluded by construction (the persisted payload is `ProjectDocument`, which has no derived fields).
- identity preservation: `TaskId`, `ResourceId`, `AssignmentId`, `DependencyId`, `BaselineId` sets are unchanged after round-trip; baseline snapshot TaskId keys, `capturedAt`, and work/cost are preserved exactly.
- the diagnostic contract: file-level errors (bad JSON, wrong magic, unsupported version) return an empty document + a single error diagnostic; entity-level errors drop the entity + emit an error diagnostic (partial recovery, explicitly represented — never silent discard); the engine's `validateProjectDocument` diagnostics are surfaced as additional error-level `ImportDiagnostic` entries.
- file security: prototype-pollution payloads (`__proto__`, `constructor`, `prototype` keys) are rejected/filtered; deeply nested structures hit the depth limit (`64`); oversized inputs hit the byte limit (`100 MiB`); no `eval` / `Function` / reviver / arbitrary-constructor deserialization path exists.
- no-regression: all accepted PROJECT-001 through PROJECT-013 tests remain green (contracts 2, engine 154, scheduling 278 — no weakened goldens, no changed formulas).
- package-boundary: the `project-foundation.yml` CI workflow greps `packages/project-file` for forbidden imports (`from 'react'`, `from 'electron'`, `from 'node:'`, `from 'http'`, `from 'https'`) alongside the other foundation packages; the grep passes with zero matches.
- architecture-lock.md unchanged; no forbidden surfaces touched (`apps/sheets`, `apps/web/src/screens/ExcelEditor*`, `apps/web/src/screens/excel/`, `packages/xlsx-gateway`, `packages/docx-engine`).

## PROJECT-015 evidence requirements

PROJECT-015 (MSPDI import) must include at minimum:

- 18 golden fixtures M01–M18 covering: M01 minimal, M02 WBS, M03 dependencies, M04 lag/lead (minute format), M05 calendars (base + inherited), M06 calendar exceptions, M07 resources (work/material/cost), M08 assignments, M09 constraints (SNET/MFO), M10 deadlines/progress, M11 baseline, M12 multiple baselines, M13 custom fields, M14 unsupported features (recurring exception → `UNSUPPORTED_MSPDI_FEATURE`), M15 malformed references (dangling predecessor → `INVALID_MSPDI_REFERENCE`), M16 large project (60 tasks, 50 dependencies, 10 resources, 60 assignments), M17 adversarial XML (DOCTYPE entity bomb + deep nesting + malformed → `INVALID_MSPDI`), M18 lag units (correction round 1 — one FS dependency per working lag unit: minute/hour/day/week/month exact conversions + a negative day-unit lead, each asserted as exact `lagMinutes` AND as exact hand-computed `DerivedSchedule` early/late dates, slack, and criticality through `validateProjectDocument` + `schedule`).
- 38 required tests covering: (1) minimal, (2) multiple tasks, (3) WBS hierarchy, (4) summary tasks, (5) milestones, (6) FS, (7) SS, (8) FF, (9) SF, (10) positive lag, (11) negative lag, (12) calendars, (13) inherited calendar, (14) calendar exception, (15) task calendar, (16) resource calendar, (17) work resources, (18) material resources, (19) cost resources, (20) assignments, (21) constraints, (22) deadlines, (23) progress, (24) baseline, (25) multiple baselines, (26) custom fields, (27) malformed XML, (28) unsupported-feature diagnostic, (29) invalid-reference diagnostic, (30) invalid-duration diagnostic, (31) invalid-date diagnostic, (32) deterministic repeated import, (33) deterministic import after equivalent element-order variation, (34) large MSPDI project, (35) security/adversarial XML, (36) imported document passes canonical validation, (37) imported document can be scheduled, (38) imported schedule is deterministic.
- every valid golden fixture asserts: zero ERROR-level diagnostics; `validateProjectDocument` accepts (zero diagnostics); `schedule()` returns a `DerivedSchedule` with zero ERROR diagnostics; deterministic re-import (`serializeGproj(importMspdi(xml))` byte-identical across repeats).
- every invalid/diagnostic golden asserts: the expected diagnostic code (`INVALID_MSPDI` / `UNSUPPORTED_MSPDI_FEATURE` / `INVALID_MSPDI_REFERENCE` / `INVALID_MSPDI_DATE` / `INVALID_MSPDI_DURATION` / `UNSUPPORTED_MSPDI_VERSION`); the document is the empty/partial recovery form (never a crash, never a silent discard).
- determinism: `serializeGproj(importMspdi(xml))` byte-identical across repeated imports; semantically-equivalent XML element-order variations produce byte-identical canonical `ProjectDocument` output; the imported `DerivedSchedule` is byte-identical across repeated `schedule()` calls.
- identity mapping: MSPDI UID → branded id is deterministic and stable within an import (`t`+uid / `r`+uid / `a`+uid / `c`+uid / `b`+slot / `d-`+succ-`-`+pred-`-`+type); NO random IDs; `parentTaskId` reconstructed from `<OutlineNumber>` (canonical identity is NOT WBS).
- LinkLagFormat conversion proof (correction round 1 — the unit code must carry its conversion semantics, not just be validated): exact-value tests for minute (×1/10), hour (×60/10), day (×MinutesPerDay/10), week (×MinutesPerWeek/10), month (×DaysPerMonth×MinutesPerDay/10); negative lag per unit; zero lag per unit; a same-`LinkLag`-different-unit five-way discriminator (10 tenths → 1/60/480/2400/9600 minutes — fails under any blanket /10 implementation); elapsed formats (2/4/6/8/10) → `UNSUPPORTED_MSPDI_FEATURE` with the dependency retained at lag 0; percentage format (35) → `UNSUPPORTED_MSPDI_FEATURE` with the dependency retained at lag 0; sub-minute conversions → `INVALID_MSPDI_DURATION`; declared project factors honored (`<MinutesPerDay>`/`<MinutesPerWeek>`/`<DaysPerMonth>`); malformed declared factor → `INVALID_MSPDI` + documented-default fallback; end-to-end import tests asserting the exact `lagMinutes` per unit; whole-minute calendar boundaries (`HH:MM:SS` with non-zero seconds → `INVALID_MSPDI_CALENDAR` + period dropped, adjacent periods intact).
- unused-factor isolation (correction round 2 — importer-level factor validation is lazy with respect to the lag formats actually present): malformed `<MinutesPerDay>`/`<MinutesPerWeek>`/`<DaysPerMonth>` declarations that no present lag format uses produce no diagnostic (minute lag + malformed day factor → zero errors with exact lag 150; hour lag + malformed day factor → zero errors with exact lag 180; minute lag + malformed week and month factors → zero `INVALID_MSPDI` diagnostics); a malformed factor used by a present lag format emits `INVALID_MSPDI` exactly once per declaration (even across multiple using dependencies — two day lags → one diagnostic, lags 480/960) and converts with the documented default (day→480, week→2400, month→9600 for one-unit lags); month lags validate both `DaysPerMonth` and `MinutesPerDay`; a valid declared factor is applied only to the units that use it (minute lag + valid day factor → lag unaffected; day lag + valid day factor alongside malformed unused week/month factors → zero errors, declared factor applied).
- date/duration semantics (authoritative per-unit table lives in `requirements.md` — Date / duration semantics, PROJECT-015 correction round 1): ISO-8601 durations (`PT#H#M#S`) → integer `WorkingMinutes` (sub-minute seconds → `INVALID_MSPDI_DURATION`, date-part components → `UNSUPPORTED_MSPDI_FEATURE`); `<LinkLag>` is tenths of the unit declared by `<LinkLagFormat>`: 1=minute ×1/10, 3=hour ×60/10, 5=day ×`MinutesPerDay`/10, 7=week ×`MinutesPerWeek`/10, 9=month ×`DaysPerMonth`×`MinutesPerDay`/10 (factors from the `<Project>` root, MSPDI defaults 480/2400/20 when absent; malformed declarations → `INVALID_MSPDI` + default fallback, validated lazily per the lag formats actually present — correction round 2); non-whole-minute results → `INVALID_MSPDI_DURATION`; elapsed (2/4/6/8/10) and percentage (35) → `UNSUPPORTED_MSPDI_FEATURE` with the dependency retained at lag 0; naive MSPDI dates interpreted as UTC (never the system timezone); `±HH:MM` offsets converted to UTC; malformed → `INVALID_MSPDI_DATE`.
- XML security: `<!DOCTYPE>` rejected; no external/parameter-entity resolution; only the five built-in named + numeric entities resolved (any other named entity rejected); decoded-text-size cap (billion-laughs guard); depth cap (pathological-nesting guard); byte cap (100 MiB); no `eval`/`Function`/reviver/arbitrary-constructor path; adversarial payloads (entity bomb, deep nesting, malformed) → `INVALID_MSPDI` + empty document.
- no MSPDI export surface as delivered in PROJECT-015: the adapter exposed `inspect` + `import` only (PROJECT-016 later added the authorized `export` behind the same adapter boundary; the PROJECT-015-era no-export assertion was updated accordingly).
- no-regression: all accepted PROJECT-001 through PROJECT-014 tests remain green (contracts 2, engine 154, scheduling 278, project-file 160 incl. 54 `.gproj` and 106 MSPDI — no weakened goldens, no changed formulas).
- package-boundary: `packages/project-file` (including the new `src/mspdi/**` source and `tests/mspdi*`) greps clean for forbidden imports (`react`/`electron`/`node:`/`http`/`https`) — zero matches; the `project-foundation.yml` CI gate runs typecheck+test for `project-file` (which now includes the MSPDI suite).
- architecture-lock.md unchanged; no forbidden surfaces touched; no MSPDI/MPP runtime dependency introduced (the XML parser is a pure-TypeScript tokenizer shipped in the package).

## PROJECT-016 evidence requirements

PROJECT-016 (MSPDI export) must include at minimum:

- 15 golden fixtures E01–E15 covering: E01 minimal, E02 WBS hierarchy, E03 dependencies (all four types), E04 lag/lead (positive/negative/zero), E05 constraints (all eight types), E06 deadlines/progress (incl. the known-loss `physicalPercentComplete`), E07 calendars (base + derived + working/non-working exceptions), E08 resources (work/material/cost + availability), E09 assignments (all three kinds), E10 baseline, E11 multiple baselines, E12 custom fields (text/number/boolean/date + null), E13 comprehensive (hierarchy + dependencies + lags + constraints + deadlines + progress + calendars + resources + assignments + baselines + custom fields + notes + XML-significant name characters), E14 large project (60 tasks, 59 dependencies, 10 resources, 60 assignments, DFS-ordered), E15 unsupported/unrepresentable canonical state (partial inherited workingWeek, 24:00 period end, divergent baseline capturedAt, non-convention calendar identity, multiple notes, numeric-looking string custom values, unrepresentable lag, invalid uids, plus five malformed-document refusals).
- 38 required tests covering: (1) minimal export, (2) project properties, (3) WBS hierarchy, (4) summary tasks, (5) milestones, (6) FS, (7) SS, (8) FF, (9) SF, (10) positive lag, (11) negative lead, (12) zero lag, (13) all eight constraints, (14) deadlines, (15) progress, (16) calendars, (17) calendar inheritance (incl. partial-week materialization with `resolveCalendar` equivalence), (18) calendar exceptions, (19) work resources, (20) material resources, (21) cost resources, (22) assignments, (23) baselines, (24) multiple baselines, (25) custom fields, (26) deterministic XML (triple byte equality), (27) repeated export, (28) round-trip through the PROJECT-015 importer, (29) reordered canonical inputs → identical bytes (plus the sibling-order counter-proof), (30) unsupported/unrepresentable diagnostics, (31) malformed canonical document rejection, (32) no random IDs, (33) no current-time values, (34) no DerivedSchedule authority, (35) XML escaping, (36) XML parser round-trip, (37) large project export, (38) export/import schedule equivalence.
- every valid golden asserts: zero error-level export diagnostics; the export imports back with zero error-level diagnostics; `validateProjectDocument` accepts; `serializeGproj(round-tripped)` is byte-identical to `serializeGproj(original)` (E06 excepting only the disclosed `physicalPercentComplete` loss); `schedule()` produces a byte-identical `DerivedSchedule`.
- exact canonical XML bytes asserted for representative goldens (E01 full document; E04 `PredecessorLink` lag encoding `LinkLag = lagMinutes × 10`, `LinkLagFormat = 1`) so the deterministic writer cannot drift silently.
- identity mapping in reverse: `Task.uid`/`Resource.uid` → MSPDI `<UID>` verbatim; calendar/assignment/baseline ids recovered from the accepted import mapping or synthesized deterministically (smallest unused, code-point-first parse order) with `UNREPRESENTABLE_MSPDI_VALUE` warnings and consistent reference remapping (asserted end-to-end for the non-convention `'standard'` calendar id).
- hierarchy: exported `OutlineNumber`/`WBS` is the deterministic DFS projection of `parentTaskId` + sibling order; the exported hierarchy imports back into identical `parentTaskId` relationships; sibling-order sensitivity proven both ways (reorder changes XML; reordered document still round-trips exactly).
- calendar inheritance: `BaseCalendarUID` preserved (never flattened); partial inherited workingWeek materialized with `resolveCalendar` equivalence asserted (the resolved calendars are deep-equal before/after round-trip) and an `MSPDI_EXPORT_NORMALIZED` info diagnostic.
- diagnostics contract: refusal (engine-invalid document) returns zero bytes + `INVALID_MSPDI_EXPORT` + verbatim engine codes; unrepresentable lag exports 0 with `INVALID_MSPDI_EXPORT_LAG` error and the dependency retained; every known-loss field (physicalPercentComplete, multi-note collapse, task.baseline index, views/tables/filters/groups, numeric-looking string values, 24:00 period ends, divergent capturedAt, wbs re-projection, non-DFS task order) is disclosed — never silent.
- determinism proof: three repeated exports byte-identical; reorder-invariance across calendars/resources/assignments/dependencies/customFields/exceptions/availability; exporter/writer source carries no clock, no randomness, no locale-aware comparison, and no scheduling-package import (static `?raw` source guards).
- XML security/representation: explicit escaping of `&` `<` `>` CR (text) and `&` `<` `>` `"` `'` tab/LF/CR (attributes); escaped characters round-trip exactly through the accepted parser; exported XML parses through `parseXml` with the expected root/namespace/element structure; element names validated so malformed markup is impossible.
- external compatibility boundary: internal round-trip correctness and MSPDI structural correctness are established; real Microsoft Project compatibility is an EXPLICIT UNPROVEN CLAIM (no Microsoft-generated corpus available in the environment — see requirements.md PROJECT-016 "External compatibility evidence boundary").
- no-regression: all accepted PROJECT-001 through PROJECT-015 tests remain green (contracts 2, engine 154, scheduling 278, project-file 209 incl. 54 `.gproj` + 106 MSPDI import + 49 MSPDI export — zero skipped).
- package-boundary: `packages/project-file` (including `src/mspdi/**` and all tests) greps clean for forbidden imports (`react`/`electron`/`node:`/`http`/`https`); the `project-foundation.yml` gate runs typecheck + test for `project-file`.
- architecture-lock.md unchanged; no forbidden surfaces touched; no MPP work as delivered in PROJECT-016 (the subsequently authorized PROJECT-017 investigation added no production MPP code — see the PROJECT-017 section below).

## PROJECT-017 evidence requirements

PROJECT-017 (MPP adapter feasibility — investigation only, no production code) must include at minimum:

- the report `spec/project/mpp-feasibility.md` with all 20 sections mandated by the brief, present and in order (executive conclusion; format overview; version matrix; candidate approaches; library/tool matrix; licensing matrix; runtime matrix; security analysis; fidelity matrix; MSPDI intermediary analysis; corpus plan; determinism analysis; performance considerations; architecture impact; recommended strategy; PROJECT-018/019 proposed scopes; rejected alternatives; open questions; source links), ending with exactly one allowed decision from the four-option set — the chosen decision being FEASIBLE — MSPDI/INTERMEDIARY ADAPTER (MPP import only; MPP export rejected).
- primary spike evidence (commands and outputs recorded in the PROJECT-017 worklog entry; report [S17]; re-verified by a second independent run of every dynamic claim): `@byteink/mppjs` headless-Linux failure (`UnsatisfiedLinkError: No awt`); MPXJ 16.7.0 distribution (mpxj.jar + 31 lib jars incl. Apache POI); 7 real corpus files verified CFB magic (MPP8/MPP9/MPP14); `UniversalProjectReader` + `MSPDIWriter` conversion of all 7 with the recorded counts; import of every generated MSPDI through the accepted `importMspdi` with byte-identical re-imports (`serializeGproj`); diagnostics surfacing the five normalization findings N1–N5 (sentinel `-1` references, `MISSING_BASE_CALENDAR`, hidden `UID 0`/`OutlineLevel 0` placeholder tasks via `INVALID_OUTLINE_LEVEL`, midnight-wrapping periods via `INVALID_MSPDI_CALENDAR`, `-65535` unassigned assignments); repeat-run re-conversion proving data-byte stability except the non-semantic `<CurrentDate>` save timestamp.
- discipline suite `packages/project-file/tests/mpp-feasibility.test.ts` (13 tests): report substantiality/structure/order; the single allowed decision on the closing line; version matrix, N1–N5, Tier A–D, numbered source links, corpus plan (pinned/618/LGPL), MPP-export rejection basis, and the PROJECT-048 performance handoff all asserted present; NO production MPP implementation (runtime dependencies exactly `@genoffice/project-contracts` + `@genoffice/project-engine`; no MPP/MPXJ/Aspose package anywhere in dependencies/devDependencies; no `src/mpp/**` module and no `MppAdapter` on the index surface); architecture-lock §13 forbidden-import guard text present verbatim plus `Status: FROZEN`.
- no-regression: all accepted PROJECT-001 through PROJECT-016 tests remain green (contracts 2, engine 154, scheduling 278, project-file 222 incl. 54 `.gproj` + 106 MSPDI import + 49 MSPDI export + 13 MPP feasibility — zero skipped).
- formatting: prettier-clean on every file the increment touches (spec + test), consistent with the repo-wide format gate.
- architecture-lock.md unchanged; no forbidden surfaces touched; no production MPP code; the spike workspace stayed outside the repository; the investigation authorizes nothing beyond itself (PROJECT-018/019 require explicit Principal Architect authorization of the recommended strategy and scope).

## PROJECT-018 evidence requirements

PROJECT-018 (MPP import via the externalized MPXJ sidecar) must include at minimum:

- foundation contract: `packages/project-file/src/mpp/**` (types/constants, N1–N5 normalizations, staged diagnostics, `importMppFromMspdi`) — pure TypeScript, zero `node:` imports, covered by the unchanged CI foundation boundary grep; surfaced through `packages/project-file/src/index.ts`.
- N1–N5 unit suite (25 tests, `tests/mpp-normalize.test.ts`): every normalization has a synthetic MPXJ-convention payload AND a raw-payload negative control proving the accepted importer fails without the normalization (the normalization removes an observed, real failure mode); severity/stage/entityId contracts; identity pass-through (no normalization → byte-identical, zero diagnostics); malformed-XML pass-through (importer's error, not the normalizer's); determinism; escape-preserving tree re-serialization round-trip; `importMppFromMspdi` staging (sidecar short-circuit, mspdi-stage INVALID_MSPDI, canonical-rejection atomicity, recoverable-error passthrough).
- documented PROJECT-015 minimal correction: `mspdiTimeToMinutes` accepts exactly `24:00:00` → 1440 (ISO-8601/XSD day-end); the pre-existing malformed-times assertion updated to nearest neighbors (`25:00:00`, `24:00:01`, `24:30:00`) with a dedicated boundary test; full 015 suite green with no other change.
- host package `packages/project-mpp-host`: protocol frame validator (7 tests), failure-injection suite (24 tests) driving the REAL process manager through a `commandBuilder` seam — sidecar unavailable (both the direct spawn-error branch under `'off'` and the wrapped branch, where the wrapper's exit 127 + "failed to execute" stderr maps precisely back to `MPP_SIDECAR_UNAVAILABLE`, never a misleading "conversion exited" — probe-aware: on hosts without the mechanism, e.g. CI runners that restrict unprivileged user namespaces, the wrapped branch asserts the fail-closed `MPP_SIDECAR_NETWORK_ISOLATION_UNAVAILABLE` refusal instead, and the 127-mapping itself stays covered by the deterministic fake-unshare behavioral test), timeout (SIGTERM escalation; the kill path proven THROUGH the isolation wrapper under the default policy, probe-aware), nonzero exit + stderr tail, invalid frame, request-id mismatch, `UNSUPPORTED_MPP_FORMAT` frame mapping, missing output file, oversized output (file removed), banner-noise tolerance, success path; pipeline-level: input-size limit enforced BEFORE any process, malformed sidecar MSPDI (mspdi stage), canonical rejection (atomic), sidecar failure propagation (sidecar-only stages), scheduled success path, input-file immutability + temp-workspace cleanup, scheduling-diagnostic staging, `buildSidecarCommand` argv-form proof, hostile-filename behavioral counter-proof (no `PWNED` artifact); input-diagnostic provenance trio (architect-review correction): a MISSING input file (`MPP_INPUT_UNREADABLE`, ENOENT preserved, never mislabeled as `MPP_INPUT_TOO_LARGE`, nothing built or started), a PERMISSION-DENIED input (`MPP_INPUT_UNREADABLE`, EACCES preserved — the input gate opens the file for reading, since stat alone needs no read permission on the file itself), and an oversized input FILE (`MPP_INPUT_TOO_LARGE`, distinct from unreadable, refused before any process).
- architecture discipline (12 tests): launcher and network-isolation sources never enable a shell / never exec; import pipeline never spawns; no renderer/Electron imports (incl. the network-isolation module); foundation never references the host package; foundation mpp surface has no process imports; Java sidecar source has no network/subprocess APIs and frames its network posture as application-level (the OS-level denial is the launcher's job); requirements.md documents the enforced network-isolation semantics in lockstep with the implementation (Option A — mechanism, `'required'`/`'off'` policies, fail-closed code; the old "known limitation" claim must be gone); LICENSE-THIRD-PARTY.md documents the LGPL posture incl. the pinned SHA-256; the corpus manifest pins the same MPXJ version as `MPXJ_PINNED_VERSION` and covers MPP8/9/12/14; `.sidecar-deps/` is gitignored; the fetch script verifies checksums.
- network-isolation evidence suite (7 tests, `tests/network-isolation.test.ts` — the architect-review correction making "no network access" a RUNTIME invariant, Option A implemented): the wrapper form (`unshare --net --map-root-user <cmd> <args…>` as a direct argv); REAL runtime enforcement — a process launched through the actual wrapper cannot connect even to a LIVE loopback listener in the parent network namespace (kernel `ENETUNREACH`) while the identical unwrapped control CONNECTS (proving both the isolation and the test apparatus), with an honest fail-closed assertion branch on hosts that cannot provide the mechanism; the DEFAULT policy is `'required'`; fail-closed — required policy + missing mechanism yields the deterministic `MPP_SIDECAR_NETWORK_ISOLATION_UNAVAILABLE` refusal; the atomicity of that refusal (empty document, sidecar stage only, provably no sidecar ran); the explicit `'off'` opt-out runs without the mechanism; a behavioral wrap proof — a recording fake `unshare` shows the launcher actually probes with and wraps the sidecar command in the configured wrapper executable (both invocations observed).
- corpus manifest verification (5 tests): MPXJ distro in place; every corpus file present with the pinned SHA-256 AND the CFB magic; per-file provenance/expected-findings documentation; all four format families represented; pinned URL/license fields.
- real-corpus goldens I01–I12 (34 tests, `e2e-real-corpus.test.ts`, real java + pinned MPXJ + real files, run under the production `networkIsolation: 'required'` posture wherever the host provides the mechanism — the suite probes once and records which posture ran in its output; on hosts without the mechanism it explicitly opts out with a recorded notice, while the fail-closed refusal itself is proven deterministically in the network-isolation suite): I01 MPP8 / I02 MPP9 / I03 MPP12 / I04 MPP14 import to the full manifest contract (entity counts, projectFinish, expected errors — the one documented importer-level case: 4 × INVALID_MSPDI_DURATION in mpp14assignmentcustom); I05–I09 each normalization against REAL files with entity-id contracts and RAW-payload negative controls (INVALID_MSPDI_REFERENCE / MISSING_BASE_CALENDAR / INVALID_OUTLINE_LEVEL / INVALID_MSPDI_CALENDAR observed without normalization, gone with it); I10 comprehensive sample.mpp (names/uids/WBS/hierarchy/milestones/summaries, all four dependency types, ±480-minute lags, work resources, assignments, calendar inheritance chain c3→c1/c4→c2, exact derived values incl. projectFinish 2003-05-26T17:00:00.000Z and critical count, staged-diagnostics provenance, zero errors); I11 malformed inputs through the REAL sidecar (garbage / ZIP-magic / empty → deterministic MPP_UNSUPPORTED_FORMAT refusal, atomic empty document); I12 per-file repeated full-pipeline determinism (serializeGproj byte-identical, schedule JSON-identical, diagnostics identical) + .gproj save/reopen byte-stability + schedule identity (8 files).
- performance recording: per-file pipeline wall times (≈1.2–2.5 s, JVM-start dominant), `.gproj` sizes (4.5–49.5 KB), MSPDI intermediates (tens of KB), `-Xmx512m` cap — operational viability for normal projects; large-scale budgets stay with PROJECT-048.
- CI: `project-foundation.yml` 15 → 19 steps (Setup Java temurin 21 → Fetch MPP sidecar dependencies (pinned, checksummed) → Typecheck project-mpp-host → Test project-mpp-host); all prior steps unchanged and green.
- no-regression: contracts 2, engine 154, scheduling 278, project-file 248 (54 gproj + 106 MSPDI import + 49 MSPDI export + 25 MPP normalization + 13 MPP feasibility + 1 24:00:00 boundary), project-mpp-host 89 (7 protocol + 24 launcher/failure-injection incl. the input-diagnostic trio + 12 architecture + 5 corpus + 34 real-corpus e2e + 7 network-isolation) — ZERO skipped.
- architecture-lock.md unchanged; no forbidden surfaces touched (change surface = packages/project-file/{src,tests}/** + packages/project-mpp-host/** + spec/project/** + .github/workflows/project-foundation.yml + root package-lock.json for the workspace link); no MPP writer, no Aspose, no COM automation; PROJECT-019 remains blocked pending rescoping.

## PROJECT-019 evidence requirements

PROJECT-019 (MPP export strategy / rescope — 019A; investigation only, no production code) must include at minimum:

- the report `spec/project/mpp-export-strategy.md` with all 19 mandated sections, present and in order (executive conclusion; current PROJECT-017 evidence; candidate strategies; runtime matrix; licensing matrix; security analysis; fidelity matrix; determinism analysis; MSPDI intermediary analysis; COM analysis; commercial SDK analysis; browser/server implications; product/distribution implications; architecture impact; recommended strategy; explicit rejected alternatives; proposed rescope of PROJECT-019; open questions; evidence/source links), ending with exactly one allowed strategy decision from the brief's A–E outcome set — the chosen decision being MPP EXPORT DEFERRED — MSPDI/.gproj IS THE SUPPORTED WRITE PATH.
- evidence currency: every external export claim re-verified against live primary sources for this increment, recorded as numbered source links [E1]–[E20] in the report (MPXJ FAQ no-writer statement; MPXJ write-howto surface with no write-MPP page; MPXJ latest release; Microsoft file-formats page designating XML as the Project interchange format; KB 257757 server-automation prohibition; Microsoft.Office.Interop.MSProject reference; Project-for-the-web no-MPP-export answer; Aspose licensing/pricing pages).
- fidelity: the export-tier matrix (Tier A exactly representable / B with documented normalization / C with explicit warnings / D not representable) mapping all major ProjectDocument feature families, with the MSPDI column grounded in accepted PROJECT-016 evidence and the MPP column explicitly marked conditional/unproven; the report must state exactly what "MPP export supported" would mean under the only realistic (conditional) strategy.
- determinism: the report must state that byte-deterministic MPP output is unproven for every candidate writer and impossible for COM (save timestamps; no suppression mechanism), and must document the round-trip oracle (`ProjectDocument → MSPDI → converter → MPP → PROJECT-018 import pipeline → ProjectDocument′`, canonical byte-equality across repeated runs) as the mandatory verification instrument for any future licensed MPP-export spike.
- discipline suite `packages/project-file/tests/mpp-export-strategy.test.ts`: report substantiality/structure/order; the single allowed decision on the closing line; the required content markers (runtime matrix, licensing matrix, fidelity tiers, determinism oracle, rejected alternatives, rescope with 019A/019B/019C, blocked-until-approval statement, numbered source links); NO production MPP writer (no MPP-writer source module anywhere under `packages/`, no `exportMpp`/MPP-export surface on any public index, the mspdi/mpp adapter surfaces unchanged in import/export shape); no commercial SDK or COM dependency anywhere in `project-file` (runtime deps exactly `@genoffice/project-contracts` + `@genoffice/project-engine`, no aspose/com/interop package in dependencies or devDependencies); the frozen architecture-lock §13 guard verbatim; PROJECT-019 remains explicitly blocked until rescope approval (requirements.md PROJECT-019 section carries the blocked-until-acceptance rule).
- zero-production-change proof: the increment's entire change surface is `spec/project/**` + `packages/project-file/tests/mpp-export-strategy.test.ts` (+ prettier-only table reflow in `work-items.md`); no file under any `src/` directory is modified.
- no-regression: all accepted PROJECT-001 through PROJECT-018 tests remain green (contracts 2, engine 154, scheduling 278, project-file 274 = the accepted 248 + the 26-test strategy discipline suite, project-mpp-host 89) — zero skipped.
- formatting: prettier-clean on every file the increment touches, consistent with the repo-wide format gate.
- architecture-lock.md unchanged; no forbidden surfaces touched; no MPP export API introduced; the investigation authorizes nothing beyond itself (019B/019C require explicit Principal Architect authorization after acceptance of the 019A decision record).

## PROJECT-020 evidence requirements

PROJECT-020 (import compatibility diagnostics) must include at minimum:

- the compatibility layer `packages/project-file/src/compatibility/**` (model, classification knowledge base, deterministic aggregator, the three pipeline entry points), surfaced through `packages/project-file/src/index.ts`; pure TypeScript, zero `node:` imports, covered by the unchanged CI foundation boundary grep.
- the golden suite `packages/project-file/tests/compatibility.test.ts` (47 tests) + `tests/compatibility-goldens.ts` (fixtures D01–D19): the 40 required tests of the brief (clean/malformed/unsupported `.gproj`; clean/unsupported-feature/malformed-date/malformed-duration/invalid-reference/calendar-degradation/baseline-warning MSPDI; clean + N1–N5 + unsupported-version + unreadable + oversized + sidecar-unavailable/timeout/network-isolation-unavailable/response-invalid/malformed-MSPDI MPP; canonical-validation failure; scheduling failure via an injected failing runner; provenance, severity classification, data-loss classification, deterministic ordering, deterministic de-duplication policy, 3×-repeat determinism, reorder stability, save eligibility both ways, summary counts, stage separation, entity provenance, no-silent-loss for N1–N5, fatal-never-authoritative) PLUS the discipline guards: classification-table completeness over every exported code family AND lockstep with the engine + scheduling sources (raw-source scan), unknown-code determinism, architecture boundary (no scheduling package, no `node:`/React/Electron, no `localeCompare`/clock/randomness in the compatibility sources), security (no env/credentials/host paths/process internals in reports), aggregation purity at scale (4000 diagnostics, byte-identical repeat). Every golden asserts the COMPLETE diagnostic object (code, severity, message, format, stage, loss, recoverability, entityType, entityId) — never just a code.
- the two additive MSPDI provenance diagnostics (`MSPDI_PHYSICAL_PERCENT_COMPLETE_DROPPED`, `MSPDI_BASELINE_CAPTURED_AT_APPROXIMATED`) — justified as the minimal parser change required to expose accurate provenance for previously-silent limitations; no accepted code renamed, no mapping changed, full accepted suites green unchanged.
- the sidecar frame completion (`format` on the success frame — the field the protocol contract always declared) + the corpus manifest `detectedFormat` values (byte-true container generations, empirically probed via MPXJ; SubprojectA-9.mpp is an MPP9-era name but an MPP14 container).
- the real-corpus integration `packages/project-mpp-host/tests/e2e-compatibility.test.ts` (9 tests, real java + pinned MPXJ + the 8 PROJECT-018 corpus files, under the enforced network-isolation posture wherever the host provides the mechanism): per file — the manifest's N1–N5 counts as classified normalization-stage diagnostics (never silent, loss ≠ none), the expected error count as classified partial-recovery errors, the three status dimensions, authority + save eligibility, the detected source format, and a second full run giving byte-identical report JSON, canonical document, and DerivedSchedule; plus the family-coverage assertion (MPP8/MPP9/MPP12/MPP14 all present).
- CI stability: the host package's vitest config sets `fileParallelism: false` (sequential test files — at most one JVM conversion pipeline on the runner at any time; eliminates the cross-file contention behind the documented transient timeouts), and the compatibility e2e uses an explicit 120 s per-test timeout for its two-pass determinism runs.
- no-regression: all accepted PROJECT-001 through PROJECT-019A tests remain green (contracts 2, engine 154, scheduling 278, project-file 321 = the accepted 274 + the 47-test compatibility suite, project-mpp-host 98 = the accepted 89 + the 9-test compatibility e2e) — ZERO skipped.
- architecture-lock.md unchanged; no forbidden surfaces touched; no MPP export reintroduced (the 019A decision record stands); the layer authorizes nothing beyond itself (renderer/host consumption is PROJECT-021+).

## PROJECT-021 evidence requirements

PROJECT-021 (shared renderer core) must include at minimum:

- the implemented `packages/project-renderer-core/src/**` boundary: `state.ts` (view state model + reconciliation), `intents.ts` (closed intent union), `reduce.ts` (pure deterministic reducer), `timeline.ts` (viewport math + day/week/month time axis), `projection.ts` (document/schedule projection with the verbatim by-reference `TaskSchedule` join), `commands.ts` (structural command builders + identity allocation), `session.ts` (the renderer session controller with injected `ScheduleRunner` and snapshot undo/redo), surfaced through `src/index.ts`; pure TypeScript, zero forbidden imports, covered by the extended CI foundation boundary grep.
- runtime dependencies exactly `@genoffice/project-contracts` + `@genoffice/project-engine`; the scheduling package consumed ONLY through the injected structural runner (test-layer import of `schedule` for the real-authority integration tests, per the accepted project-file precedent).
- the component suite `packages/project-renderer-core/tests/**`: view-state creation/reconciliation (dead-reference dropping incl. anchor/focus; the anchor/focus SELECTION invariant — anchor/focus are validated against the surviving `taskIds`, not mere document existence: live-but-unselected anchor/focus dropped (incl. the malformed externally-restored-state counterexample `taskIds=[a,b], anchorId=c` with live `c`), a selected task deleted → anchor/focus reconciled independently with surviving members kept, and an invariant battery proving `anchorId === undefined || taskIds.includes(anchorId)` (likewise `focusId`) across malformed/edge states, mirrored through the reducer's automatic post-intent reconciliation from a malformed state; dependency/resource selections, active view refs), reducer semantics (set/toggle/extend selection incl. anchor behavior and outline-order ranges, selectTasks validation order, collapse intents incl. collapseAll = exactly the summaries and the enforced invariant `collapsed ⊆ summary TaskIds` — leaf ids are deterministic no-ops in toggleCollapse and filtered in setCollapsed, and reconciliation prunes leaf entries incl. restored host state containing leaves and a collapsed summary whose subtree was deleted, viewport set/scale/fit with span clamps and focus-stationary scaling, active view set/clear, deterministic no-ops for unknown references, 3×-repeat determinism, immediate reconciliation against a smaller document incl. anchor/focus), timeline math (fraction mapping + inverse + clamping, halve/double round-trip, min/max span clamps, project window precedence schedule → properties → 30-day default, fit padding 2% with one-day minimum, axis level thresholds, contiguous clipped day bands, Monday-aligned weeks, calendar months, month level for multi-year spans), projection (outline-order rows, collapse hiding incl. nested ancestors, verbatim task-field echoes, resourceNames document-order dedup, by-reference schedule join — `toBe` identity, undefined schedule fields never invented, schedule-diagnostics echo, project-window precedence, active view resolution, 3×-repeat determinism + input immutability, 2000-task linearity), structural builders (identity allocation incl. reversed arrays, creation defaults, last-root/last-child insert positions accepted by the engine — the discriminated `TaskInsertPosition` union expresses exactly the executable CreateTask positions and the suite carries the NON-LAST-ANCHOR golden (three siblings p > a1,a2,a3, anchor a2: the new task appends after a3, explicitly NOT immediately after a2, proving the frozen command layer has no row-position insert) plus the root-anchor append-after-last-root case and the unknown-anchor disabled gesture, indent via the engine's preceding-sibling rule incl. the cousin case, outdent eligibility, subtree-absorbing delete selection in reverse outline order, unknown-id determinism), and session control (initial schedule through the injected runner, accepted-command journaling + rescheduling + immutability, rejected commands returning the same reference with surfaced diagnostics and no consumed command id, schedule tracking of live documents, no-runner operation, byte-exact snapshot undo AND redo incl. the deliberately non-invertible `OutdentTask`/`CreateBaseline`, redo-future clearing on new commands, no-op undo/redo, plain journal entries, and a 3×-repeat end-to-end determinism run over commands + undo + redo with the REAL scheduling package).
- the architecture discipline suite `tests/architecture.test.ts`: package.json dependency exactness; raw-source scans over every `src/*.ts` module for React/Electron/Node/HTTP/scheduling/file/host imports; no `Date.now`/`Math.random`/`localeCompare`; view-state purity (no `TaskSchedule` types or scheduling-derived field declarations in state/intents); public-surface presence on the index; the scheduling-injection documentation on the session module; CI lockstep (boundary grep + typecheck + test steps covering the package); spec lockstep (requirements/work-items/dependency-graph/verification-matrix carry PROJECT-021); architecture-lock §3 renderer boundary verbatim and FROZEN. The suite itself uses only vitest + `?raw` module sources (no `node:` imports — CI-grep-safe).
- CI: `project-foundation.yml` 19 → 21 steps (`Typecheck project-renderer-core` + `Test project-renderer-core` after the project-file steps; the foundation boundary grep extended to `packages/project-renderer-core`); correction-round CI-stability measure: `e2e-real-corpus.test.ts` carries explicit per-test timeouts (30 s single-pass conversions, 120 s two-pass I12 determinism — the e2e-compatibility precedent) because the file's first cold JVM conversion exceeded the 5 s vitest default on slow shared pull_request runners (observed twice at ~5.01 s) while the identical suite passed on the push runner and locally; test-only change, no production code.
- no-regression: all accepted PROJECT-001 through PROJECT-020 tests remain green (contracts 2, engine 154, scheduling 278, project-file 321, project-mpp-host 98) — ZERO skipped.
- formatting: prettier-clean on every file the increment touches.
- architecture-lock.md unchanged (0-line diff — §3 already sanctions the renderer-core boundary); no forbidden surfaces touched; no host UI/Electron/web code (complete views are PROJECT-022+ and remain unauthorized until acceptance of this increment).

## PROJECT-022 evidence requirements

PROJECT-022 (Gantt / task grid / timeline views) must include at minimum:

- the implemented `packages/project-renderer-core/src/views/**` boundary: `virtualization.ts` (the shared row window + clamping), `grid.ts` (the `ProjectTaskGrid` column/cell model), `gantt-bars.ts` (bar geometry + progress interpolation), `milestones.ts` (diamond geometry), `dependencies.ts` (link geometry + endpoint resolution + the elbow route), `timeline.ts` (the composed `ProjectTimeline`), `gantt-view.ts` (`buildGanttView` + `hitTestGantt`), surfaced through `src/index.ts`; pure TypeScript, fraction-space geometry only, zero forbidden imports, covered by the existing CI foundation boundary grep.
- runtime dependencies unchanged: exactly `@genoffice/project-contracts` + `@genoffice/project-engine`; the scheduling package still consumed ONLY through the projection's by-reference schedule join (and at the TEST layer for the real-authority integration test).
- the component suite `packages/project-renderer-core/tests/views/**`: virtualization (plain/clamped/overscaped windows, the canonical empty window, degenerate inputs, 3× determinism); task grid (default columns; active-table resolution incl. the `.gproj` field-name convention; unknown fields → unsupported cells; empty-table fallback; structured cells for every supported field — rowNumber CANONICAL and stable under collapse, taskName with indent/expander data, duration/percent with the schedule-first precedence and task-field fallback, start/finish instants EMPTY without a schedule, predecessors in document order with uid/type/lag, resourceNames, wbs/outlineLevel/priority/uid; by-reference row joins; window slicing with absolute indices; empty window; purity 3× + input immutability; 2000-row linearity); Gantt bars (fraction mapping in ascending row order, summary kind from the rolled-up window, linear progress interpolation incl. the task-field fallback, viewport clipping with flags on both edges, NO bar without a complete span, no bar for zero spans, nothing for empty windows/degenerate viewports, window slicing, purity); milestones (flagged diamond at scheduledStart, unflagged zero-duration, zero-span schedule, BOTH bar and diamond for a flagged milestone with a real span, none without a schedule, out-of-viewport flags + clamping, window slicing, purity); dependency links (the four edge anchors FS/SS/FF/SF with the four-point elbow route and midpoint xm, canonical endpoint ids preserved after ancestor resolution, same-row omission inside a collapsed subtree, scrolled-out-window omission with in-window links surviving, NEAREST-visible-ancestor attachment, omission when the resolved row lacks the needed instant, nothing for empty windows/degenerate viewports, document order, purity); timeline composition (viewport echo by reference, deterministic axis bands at the span-derived level, in-window rows with absolute indices joined by reference, all three geometry surfaces over the SAME window, collapse reflected through every surface, EMPTY model for a degenerate viewport, purity); and the composite gantt view (ONE shared row-window object across both panes — synchronization by construction, single clamping of layout inputs, full composition, empty document, no-schedule document, the view-state viewport, the REAL-scheduling-package end-to-end join incl. grid instant cells, purity; hit testing: bar containment, milestone-before-bar priority, host tolerance, clean misses).
- the architecture discipline suite extended: the raw-source scan globs `src/**/*.ts` (views covered); the public-surface lockstep carries the PROJECT-022 exports incl. the five architect-named surfaces as exported types; a views-specific guard against pixel/DOM layout APIs (`clientWidth`/`offsetWidth`/`getBoundingClientRect`/…, DOM document access), wall clock, randomness, and `localeCompare`; spec lockstep (requirements/work-items/dependency-graph/verification-matrix carry PROJECT-022); CI lockstep unchanged (the existing 21-step gate covers the package).
- no-regression: all accepted PROJECT-001 through PROJECT-021 tests remain green (contracts 2, engine 154, scheduling 278, project-file 321, project-renderer-core 101 → 166+, project-mpp-host 98) — ZERO skipped.
- formatting: prettier-clean on every file the increment touches.
- architecture-lock.md unchanged (0-line diff — §3 sanctions the renderer-core boundary "until PROJECT-021+"); no forbidden surfaces touched; no host UI/Electron/web code (the shells are PROJECT-027/028, which the dependency graph places after 022 — browser/desktop E2E for this surface lands with them, when a DOM exists to drive; the pure view models have no DOM to E2E).

## PROJECT-023 evidence requirements

PROJECT-023 (Selection / editing) must include at minimum:

- the implemented engine commands in `packages/project-engine/src/commands.ts`: `mutateForSetTaskDuration` (leaf-only; `MISSING_TASK` / `SUMMARY_DURATION_NOT_SETTABLE` / `INVALID_DURATION` for anything outside the non-negative integer working-minute domain; zero valid; total inverse) and `mutateForSetTaskFinish` (leaf-only; `MISSING_TASK` / `SUMMARY_FINISH_NOT_SETTABLE` / `INVALID_DATE`; pins the STORED finish — an interchange echo the scheduler never reads; inverse only when a previous finish existed), both wired into the `applyProjectCommand` dispatch — no contracts change, no other command touched.
- the engine suite `packages/project-engine/tests/project-023-commands.test.ts`: accept (field set, inverse, undo-via-inverse round-trip byte-identity, zero-duration domain, input immutability + determinism), reject (missing task, summary roll-up, negative/fractional/NaN/Infinity durations, malformed dates — same-document-reference guarantees), and the REAL-scheduler effect tests (a duration edit moves the derived finish exactly as the scheduling authority computes it; the engine itself touches no dates; a stored-finish pin leaves the derived schedule byte-identical — the honest inert semantics).
- the renderer-core editing model `src/editing.ts` + suite `tests/editing.test.ts`: `editableTaskFields` (leaf full set incl. milestones; summary name-only), `initialTaskEditDraft` (name verbatim; duration schedule-first with task-field fallback; start/finish from the scheduled instants and EMPTY without a schedule — never invented), `commitTaskEdit` (name/dates verbatim → `RenameTask`/`SetTaskStart`/`SetTaskFinish`; duration parsed under the strict canonical decimal rule with negative/fractional values still dispatched for the ENGINE to reject; the unparseable-text battery; `noChange` against the STORED field incl. the displayed-value-still-pins case; `missingTask`; `none`; purity + input immutability).
- the reducer extensions + `tests/reduce.test.ts` additions: `moveTaskFocus` (bootstrap from nothing; up/down/first/last; reference-equal clamped no-ops; collapse-aware visible order incl. focus on a collapsed summary jumping past its hidden subtree; extend via the accepted outline-range rule; the anchor/focus invariant battery after every step; empty-document no-op) and the editing intents (`beginTaskEdit` activates with the canonical draft from the reducer context and SELECTS the edited row; summary scheduling fields and unknown tasks are reference-equal no-ops while the summary NAME edits; one editor at a time; `updateTaskEditDraft`/`endTaskEdit` no-op discipline; reconciliation drops a dead-target edit; 3× determinism).
- the commit flow `src/edit-flow.ts` + suite `tests/edit-flow.test.ts` (the COMMAND AUDIT — the work-item's verification, over the REAL scheduling package at the test layer): the full pipeline test (gesture → intent → `RenameTask` → engine → journal `c1` → new document → reschedule → editor end → selection preserved → projection refresh shows the edit), the duration edit moving the derived finish through the scheduler, the start edit pinning the candidate, the stored-finish pin leaving the derived schedule honest, the unparseable draft (no command dispatched), the ENGINE-rejected edit (`INVALID_DURATION` surfaced verbatim, same session reference, editor ended, selection preserved), `noChange` (no journal entry), `none` (same references), undo/redo over committed edits (byte-identical document AND schedule restoration), multi-selection surviving a committed edit, the missing-task commit defense, and 3× byte-identical determinism of the whole chain.
- the projection reflection + view integration `src/projection.ts` (rows gain the additive `selected`/`focused`/`editingField` state echoes — grid and timeline rows carry the projection rows by reference, so every PROJECT-022 view model is selection-aware with no second join; the DRAFT stays live state, only the edit TARGET projects) + suite `tests/integration.test.ts`: the reflection echo battery (selected/focused flags; the edit target; 3× projection determinism), grid row → `selectTask` → flags in the rebuilt synchronized panes, bar hit → `selectTask` → the selected row, milestone hit → `selectTask` → the selected row, the hidden-selection policy (hiding is not deselecting: a selected child stays selected while hidden and re-projects on expand), the outline range across a collapsed summary keeping hidden members selected, selection preservation through task creation (the new row unselected — the documented policy), indent/outdent (ids survive, selection verbatim), and the UNSUPPORTED_COMMAND path (`AddDependency` rejected, same session reference, document and selection untouched).
- the golden scenario battery `tests/goldens.test.ts` (S01–S12 over the REAL scheduling package): S01 single selection, S02 multi-selection, S03 range selection, S04 collapsed hierarchy selection, S05 keyboard range, S06 rename (stable TaskId/uid/hierarchy/dependency/baseline identity, schedule recomputed, selection preserved), S07 percent completion (0/partial/100 accepted, out-of-range rejected `INVALID_PERCENT_COMPLETE` with the same document reference, summary rejected — derived), S08 rejected edit (engine `INVALID_DURATION`, same session reference, editor ended, selection preserved), S09 edit + reschedule (derived finish moves through the scheduler), S10 undo/redo (document + schedule + selection restored exactly), S11 delete-selected reconciliation (reverse-outline command order, dangling dependencies cleaned by the engine, selection pruned deterministically), S12 the 3× byte-identical repeated interaction sequence — each golden asserts the complete observable tuple (selection taskIds/anchorId/focusId, edit target where applicable, ProjectDocument, DerivedSchedule, projection state).
- the architecture discipline suite extended: the public-surface lockstep carries the PROJECT-023 exports (`EditableTaskField`, `TaskEditing`, `TaskEditCommit`, `TaskEditInvalidReason`, `TaskEditFlowOutcome`, `MoveFocusDirection`, `editableTaskFields`, `isTaskFieldEditable`, `initialTaskEditDraft`, `commitTaskEdit`, `commitTaskEditThroughSession`); the intent-union and editing-slice source guards; the lock §11 scheduling-free scan now covers `editing.ts` + `edit-flow.ts`; spec lockstep for 023 across requirements/work-items/dependency-graph/verification-matrix; CI lockstep unchanged (the existing 21-step gate covers both touched packages).
- no-regression: all accepted PROJECT-001 through PROJECT-022 tests remain green (contracts 2, engine 154 → 170, scheduling 278, project-file 321, project-renderer-core 167 → 219 → 243, project-mpp-host 98) — ZERO skipped.
- formatting: prettier-clean on every file the increment touches; `architecture-lock.md` unchanged (0-line diff).

## PROJECT-024 evidence requirements

PROJECT-024 (Dependency editing) must include at minimum:

- the implemented engine commands in `packages/project-engine/src/commands.ts`: `mutateForAddDependency` (append in canonical order; the full early-rejection matrix — `DUPLICATE_DEPENDENCY_ID` / `MISSING_TASK_REFERENCE` / `SELF_DEPENDENCY` / `INVALID_LAG` / `DUPLICATE_DEPENDENCY_LINK` / `SUMMARY_DEPENDENCY` both directions / `DEPENDENCY_CYCLE` via deterministic reachability over the existing edges — with the post-command validator as the safety net; inverse `RemoveDependency`), `mutateForRemoveDependency` (identity filter; `MISSING_DEPENDENCY`; the FULL-record `AddDependency` inverse), `mutateForChangeDependencyType` (`MISSING_DEPENDENCY` / `INVALID_DEPENDENCY_TYPE` runtime domain guard / `DUPLICATE_DEPENDENCY_LINK` re-key collision; same-type change accepted idempotent; inverse the previous type), and `mutateForChangeLag` (`MISSING_DEPENDENCY` / `INVALID_LAG` non-integer; negative = lead accepted; inverse the previous lag) — all four wired into the `applyProjectCommand` dispatch; no contracts change, no other command touched; the frozen union is now FULLY implemented (the `UNSUPPORTED_COMMAND` default keeps its runtime safety-net role against malformed values).
- the engine suite `packages/project-engine/tests/project-024-commands.test.ts`: accept (append/shape, inverses, undo-via-inverse byte-identity including the full-record removal round-trip, affectedTaskIds = both endpoints, input immutability + determinism), the complete rejection battery (duplicate id, missing references either endpoint, self link, non-integer lag, duplicate link, summary↔descendant both directions, DIRECT cycle, TRANSITIVE three-edge cycle — same-document-reference guarantees on every rejection), the idempotent same-type change, and the REAL-scheduler effect tests (FS moves the unpinned successor to the next working instant; +480 lag one further working day; −480 lead back to the predecessor start; FS→SS moves the successor to the predecessor start; the lag change and the removal restoring the project-start schedule; the engine itself touches no task fields).
- the renderer-core dependency builders `src/commands.ts` + suite `tests/dependency-commands.test.ts`: `nextDependencyIdentity` (`d1` from empty; max+1; ignores non-`d{n}` ids like the MSPDI `d-{succ}-{pred}-{type}` scheme — never renumbers imported links; deterministic), `buildAddDependencyCommand` (canonical defaults FS/0; explicit type/lag honored; identity allocated over existing links; `undefined` for unknown endpoints and self-reference — the disabled gesture, never an invented command), `buildRemoveDependencySelectionCommands` (one command per EXISTING id in canonical document order; unknown ids dropped; empty selection → []).
- the dependency-editing model `src/dependency-editing.ts` + suite `tests/dependency-editing.test.ts`: both fields editable on every dependency; the canonical drafts (type code verbatim, lag decimal minutes; empty for a missing dependency); `commitDependencyEdit` (the four type codes → `ChangeDependencyType`; anything else `unparseableDependencyType` incl. lowercase/whitespace/labels; the canonical-decimal lag rule with negative and FRACTIONAL values dispatched for the ENGINE to reject — `INVALID_LAG` — the `SetTaskDuration` precedent; `noChange` against the stored field; `missingDependency`; `none`; purity); the reducer intents (`beginDependencyEdit` activates with the canonical draft and SELECTS the link; unknown dependency is a reference-equal no-op; `updateDependencyEditDraft`/`endDependencyEdit` no-op discipline; the SINGLE-EDITOR rule across the task and dependency families in both directions; reconciliation drops an edit whose link died).
- the dependency commit flow `src/edit-flow.ts` + suite `tests/dependency-edit-flow.test.ts` (the COMMAND AUDIT — the work-item's verification, over the REAL scheduling package at the test layer): the accepted lag edit (journaled `c{n}`, document + schedule replaced — the successor's derived start moves exactly as the scheduler derives — editor ended, the edited link stays selected), undo/redo (byte-identical document AND schedule restoration, selection preserved), the ENGINE-rejected edit (`INVALID_LAG` surfaced verbatim, same session reference, editor ended, selection untouched), `noChange` (no journal entry), the unparseable draft (no dispatch), `none` (same references), and 3× byte-identical determinism of the whole chain.
- the link interaction-state reflection `src/views/dependencies.ts` + `src/views/timeline.ts` + `src/views/gantt-view.ts` (links gain the additive `selected`/`editingField` echoes through `buildDependencies`' ADDITIVE optional `state` parameter threaded from `buildGanttView`; the DRAFT stays live state, only the edit TARGET projects; geometry identical with and without the state — the 022 link tests pass unchanged) + suite `tests/dependency-integration.test.ts`: the full semantic path (gesture → builder → command → engine → document → scheduler → projection + link-surface refresh), the link refresh for every mutation (create appears; FS→SS re-routes the edge anchors finish→start to start→start; lag keeps the link and moves the successor bar; remove drops it), the reflection echo + the edit-target projection + the draft-stays-live rule, reflection geometry-neutrality and 3× determinism, the cycle rejection through the session (same session reference, document and selection untouched, the selected link still projects), the self-reference and invalid-reference disabled gestures, selection preservation through create (task AND dependency selections verbatim) / remove (the removed link's selection pruned by the reconcile pass, task selection surviving) / edit (both selections preserved), and the deterministic 3× replay of the mixed create/retype/relag/remove sequence.
- the golden scenario battery `tests/dependency-goldens.test.ts` (D01–D12 over the REAL scheduling package): D01 create FS (defaults), D02 create FS + one-day lag, D03 create FS + one-day lead, D04 create SS (start-equality + the start→start anchors), D05 create FF (finish-pinning + the finish→finish anchors), D06 create SF with a pushing lag (the required-start bound `predStart + lag − duration` floored by the project start — the honest SF semantics), D07 lag edit through the commit flow, D08 type edit through the commit flow (link re-routes, schedule follows), D09 the rejected edit leaving the whole observable tuple unchanged, D10 the cycle-closing gesture rejected with everything unchanged, D11 undo/redo restoring document + schedule + selection after a lag edit, D12 the 3× byte-identical repeated interaction sequence (create → select → edit → commit → undo → redo → retype → remove) — each golden asserts the complete observable tuple (dependency selection, task selection, edit target where applicable, ProjectDocument, DerivedSchedule, link surface).
- the architecture discipline suite extended: the public-surface lockstep carries the PROJECT-024 exports (`AddDependencyOptions`, `EditableDependencyField`, `DependencyEditing`, `DependencyEditCommit`, `DependencyEditInvalidReason`, `DependencyEditFlowOutcome`, `DEFAULT_DEPENDENCY_TYPE`, `DEFAULT_DEPENDENCY_LAG_MINUTES`, `DEPENDENCY_TYPE_CODES`, `EDITABLE_DEPENDENCY_FIELDS`, `editableDependencyFields`, `initialDependencyEditDraft`, `commitDependencyEdit`, `commitDependencyEditThroughSession`, `buildAddDependencyCommand`, `buildRemoveDependencySelectionCommands`, `nextDependencyIdentity`); the dependency-intent and dependency-editing-slice source guards (incl. the link view model's reflection fields); the lock §11 scheduling-free scan now covers `dependency-editing.ts`; the pre-024 UNSUPPORTED_COMMAND tests updated to malformed-value payloads (the frozen union is fully implemented); spec lockstep for 024 across requirements/work-items/dependency-graph/verification-matrix; CI lockstep unchanged (the existing foundation gate covers both touched packages).
- no-regression: all accepted PROJECT-001 through PROJECT-023 tests remain green (contracts 2, engine 170 → 201, scheduling 278, project-file 321, project-renderer-core 243 → 308, project-mpp-host 98) — ZERO skipped.
- formatting: prettier-clean on every file the increment touches; `architecture-lock.md` unchanged (0-line diff).

## Architecture reconciliation evidence requirements (roadmap-lockstep increment)

The reconciliation increment (architecture-lock §13 clarification via ACR-001, work-item/dependency-graph lockstep, specification-consistency guards, strengthened boundary tests) must include at minimum:

- the architecture-change record `spec/project/architecture-changes/ACR-001-project-file-adapter-boundary.md` containing motivation, affected invariant, old interpretation, new interpretation, alternatives considered, compatibility impact, migration impact, verification impact, and the explicit Principal Architect approval reference — and the matching architecture-lock §13 clarification (the ONLY lock change, carrying the architect's verbatim clarified rule and the ACR-001 cross-reference; not a silent modification; `Status: FROZEN` retained).
- the roadmap reconciliation: PROJECT-023's direct dependency corrected to `022` in `work-items.md` (the canonical chain `021 → 022 → 023 → 024`); the dependency graph's per-item canonical direct-dependency block (the former foundation-tree and chain-shorthand blocks superseded — same direct dependencies, no roadmap reorder, no accepted dependency-semantics change); and the accepted-frontier record (at authoring: PROJECT-001..024 accepted, PROJECT-019 completed as the 019A rescope decision record, next authorized = PROJECT-025).
- the lockstep frontier advance upon PROJECT-025's acceptance (Principal Architect review of PR #26 at `210d5ad`, merged as `ffd45ac`): the accepted-frontier record advanced to PROJECT-001..025 accepted with PROJECT-026 (`Critical-path/resource visualization`, direct dependencies `022` + `012`, both accepted) as the next authorized increment, and the specification-consistency guard's next-authorized assertions advanced in lockstep (frontier contiguity 001..025, PROJECT-026 row presence and dependency match in both documents, all direct dependencies inside the accepted set).
- the deterministic specification-consistency guard `packages/project-file/tests/spec-consistency.test.ts` (source-level text parsing ONLY; vitest + `?raw` imports, no new runtime package): every numbered work item exists in both documents (item-set equality, 49 items, PROJECT-number order pinned in both); every direct dependency declared by `work-items.md` is represented in the dependency graph AND every graph edge has a corresponding work-item dependency (per-item set equality in both directions); no contradictory predecessor declarations (set equality, no self-dependencies, dependencies point to strictly lower-numbered items only, global acyclicity); no unknown work-item IDs in either document's dependency data; PROJECT-019A remains correctly represented as the accepted rescope decision (the 019 row title, the dependency-graph PROJECT-019 section's decision-record language, the frontier record — and never a separate graph node); the next authorized product increment remains correctly recorded (frontier statement, row presence in both documents, matching direct dependencies, all of them inside the accepted set — PROJECT-025 at authoring, PROJECT-026 after the lockstep advance upon PROJECT-025's acceptance).
- the strengthened architecture-boundary guards: `packages/project-file/tests/architecture.test.ts` (project-file MAY contain the `.gproj`/MSPDI/MPP adapter implementations; project-engine, project-scheduling, and project-renderer-core MUST NOT import project-file, MSPDI parser internals, or MPP parser internals — cross-package import-specifier scans; the host package may import only the project-file PUBLIC surface, never deep parser internals; exact runtime dependency sets for all four foundation packages), `packages/project-engine/tests/architecture.test.ts` and `packages/project-scheduling/tests/architecture.test.ts` (self-scans: exact dependency sets, import-specifier allowlists, no file-format knowledge — the domain engine and the scheduling engine), and the extended `packages/project-renderer-core/tests/architecture.test.ts` (explicit file-format-internals import prohibition — the renderer acquires no file-format knowledge).
- the previously merged verbatim §13 guards (`mpp-feasibility.test.ts`, `mpp-export-strategy.test.ts`) updated to assert the clarified §13 rule verbatim plus ACR-001's presence — strengthened, never weakened.
- no-regression: all accepted PROJECT-001 through PROJECT-024 tests remain green; zero skipped tests; no Excel surfaces changed; no renderer behavior, scheduling behavior, command semantics, `.gproj`/MSPDI/MPP behavior, or file-format semantics changed; PROJECT-025 NOT implemented in this increment.
- formatting: prettier-clean on every file the increment touches.

## PROJECT-025 evidence requirements

PROJECT-025 (Calendar visualization) must include at minimum:

- the calendar projection layer `packages/project-renderer-core/src/calendar.ts`: the structural `CalendarWorkingTimeQuery` type (the injected canonical working-time evaluator — the `ScheduleRunner` injection precedent; NEVER implemented in renderer-core, satisfied by the documented host-side binding `workingIntervals ∘ resolveCalendar` from `@genoffice/project-scheduling`); `classifyCalendarBands` (pure interval algebra over the evaluator's output — clip/sort/merge/complement into contiguous `[start, finish)` working/non-working bands; NO calendar semantics: no date decomposition, no exception matching, no inheritance resolution); `buildCalendarSurface` (exactly one evaluator call per surface; default id from `properties.defaultCalendarId`; degradation mirroring `schedule()` — coded evaluator errors → `status: 'unresolvable'` + the `ImportDiagnostic`-shaped echo with the SAME code the authority reports, uncoded errors re-thrown, degenerate windows → no bands without consulting the evaluator); `buildCalendarCatalog` (pure structural echoes of `ProjectDocument.calendars` by reference + declaration aggregations: working weekdays, declared minutes per weekday, weekly minutes, exception count, direct task-reference count); and the `CalendarViewInput` per-render threading type (ephemeral render state, never persisted `ProjectViewState`).
- the timeline/Gantt integration `src/views/timeline.ts` + `src/views/gantt-view.ts`: `ProjectTimeline` gains the ADDITIVE optional `calendar` (the background working-time surface) and `rowCalendars` (one `ProjectRowCalendar` per in-window row whose schedule carries the AUTHORITY's `TaskSchedule.resolvedCalendarId` — never re-derived); surfaces for identical calendar ids shared by reference within one build (a per-build memo, nothing cached across renders — lock §11); `buildGanttView`/`buildTimeline` accept the optional `CalendarViewInput`; absent query → NO surfaces (never invented); geometry byte-identical with and without the calendar input (the additive-surface contract, tested).
- the unit suite `tests/calendar.test.ts`: the catalog echo/aggregation battery (document order, verbatim references, ascending working weekdays incl. the empty-period-list-is-non-working rule, declared-minute sums, exception/task counts, out-of-range weekday keys dropped, 3× determinism); the band-classification battery (empty → single non-working band; full-window → single working band; alternation with the contiguity invariants — exact cover, adjacent boundaries, no empty bands; clipping; overlapping/adjacent merging; order independence; unparseable/empty/inverted intervals dropped; degenerate window → no bands; 3× determinism); the surface battery (query receives the calendars BY REFERENCE + the resolved id + the window; explicit-id override; name echo present/absent; the coded-error degradation with the verbatim diagnostic echo and NO bands; the uncoded-error re-throw; the non-Error coded throw; the degenerate window not consulting the evaluator; 3× determinism).
- the integration suite `tests/calendar-integration.test.ts` (the REAL `resolveCalendar` + `workingIntervals` binding — the no-second-engine evidence): band EQUALITY with the authority's own output on the same window (working bands are the evaluator's intervals verbatim; non-working the complement); the standard-week weekend shading; the split-period day; the holiday exception (fully non-working date) and the working-Sunday exception; base-calendar inheritance (child weekday override + base exception both resolving; child exception overriding the base for the same date) with the canonical-equality cross-check; viewport-zoom re-projection clipped to the new window; the full pipeline (session → projection → `buildGanttView`): the background surface + per-row surfaces with the AUTHORITY-resolved calendar ids (the compact-calendar task's row shading differs from the standard background), the surface-by-reference sharing, the explicit background-calendar choice, the no-query absence, the geometry-neutrality (bands/rows/bars/milestones/links/taskGrid byte-identical), the empty `rowCalendars` when no scheduler is wired, the diagnostic-code EQUIVALENCE with `schedule()`'s own degradation for BOTH a dangling default calendar (`MISSING_CALENDAR`) and an inheritance cycle (`CALENDAR_CYCLE`) with no schedule values anywhere, the catalog over real calendars, and 3× end-to-end determinism incl. query purity.
- the golden scenario battery `tests/calendar-goldens.test.ts` (C01–C12 over the REAL scheduling package): C01 standard week (five 09:00–17:00 bands, the contiguous complement, canonical equality), C02 split periods (morning/afternoon alternation), C03 holiday exception (no band on the declared date; the catalog echoes the declaration), C04 working-Sunday exception, C05 inheritance (the merged resolved week + the declaration-vs-resolution catalog split), C06 exception override (child replaces base for the same date; the base keeps its own), C07 per-task calendars through the real schedule (row surfaces carry `resolvedCalendarId`; the compact row's bands differ from the standard background over the same viewport), C08 the no-query absent surfaces with byte-identical geometry, C09 the unresolvable default calendar (the surface echoes the authority diagnostic; no bands; empty row surfaces), C10 viewport-zoom re-projection (clipped to the new window; canonical equality), C11 session commands moving the schedule while the calendar surfaces stay byte-identical (the calendars and viewport are untouched; undo/redo restore the exact schedules), C12 the 3× byte-identical mixed command/viewport sequence — each golden asserts the complete observable tuple (catalog where applicable, background surface, per-row surfaces, authoritative schedule dates).
- the architecture discipline suite extended: the public-surface lockstep carries the PROJECT-025 exports (`CalendarWorkingInterval`, `CalendarWorkingTimeQuery`, `CalendarSurfaceStatus`, `CalendarViewInput`, `ProjectCalendarBand`, `ProjectCalendarCatalog`, `ProjectCalendarCatalogEntry`, `ProjectCalendarSurface`, `ProjectRowCalendar`, `CALENDAR_EVALUATION_FAILED`, `buildCalendarCatalog`, `buildCalendarSurface`, `classifyCalendarBands`); the timeline/gantt-view source guards (the additive optional surfaces; the `CalendarViewInput` threading); the NEW no-second-calendar-engine guard scanning `calendar.ts` for date-decomposition and exception-keying markers (getUTC*/Date.UTC construction/dateKey/ISOString-slicing) plus the injected-query-only shape assertions; spec lockstep for 025 across requirements/work-items/dependency-graph/verification-matrix; CI lockstep unchanged (the existing foundation gate covers the package and its boundary grep covers it recursively).
- no-regression: all accepted PROJECT-001 through PROJECT-024 tests remain green (contracts 2, engine 201, scheduling 278, project-file 321, project-renderer-core 308 → 362, project-mpp-host 98) — ZERO skipped.
- formatting: prettier-clean on every file the increment touches; `architecture-lock.md` unchanged (0-line diff).

## PROJECT-026 evidence requirements

PROJECT-026 (Critical-path/resource visualization) must include at minimum:

- the single canonical allocation kernel `packages/project-scheduling/src/allocation-kernel.ts` + the canonical allocation authority `packages/project-scheduling/src/allocation.ts`: the ONE implementation of the resource demand/capacity semantics (the demand-contribution rules, the demand-interval construction, the resource-calendar resolution, the capacity-window resolution with tightest coverage, the calendar-aware segmentation with the boundary collection incl. working-period endpoints, and the over-allocation predicate) consumed by BOTH the exported `resourceAllocations(document, schedule)` (a pure read-only projection of a GIVEN DerivedSchedule — never schedules, never mutates, never proposes commands; `schedule()` stays the sole authoritative scheduling operation, lock §6) + `ResourceAllocationSegment`/`ResourceAllocation` — EVERY segment of the tiling emitted (incl. zero-demand non-working segments) with the canonical `overallocated` flag per segment — and `levelResources()` (the leveler collapses the consecutive over-allocated runs of the SAME kernel segments into its conflict windows and leveling decisions; the leveler's PROJECT-013 observable behavior byte-identical, every L-golden passing unchanged with ZERO test modifications); determinism is the shared kernel contract (allocations sorted by resourceId, segments ascending, assignment ids sorted — invariant under array reordering and serialization round-trips).
- the canonical suite `packages/project-scheduling/tests/project-026.test.ts`: the demand-tiling battery (single assignment; the calendar-aware zero-demand night; the disjoint never-over-allocated case; the capacity-window segment split with the tightest-units drop), the over-allocation flag, the skip rules (material/cost/no-capacity/assignment-less resources; milestone/summary/zero-duration/unscheduled tasks), the determinism battery (3× repeat, reversed arrays, JSON round-trip, purity), and the LEVELER CROSS-CHECK (the union of consecutive over-allocated segments equals `levelResources().overallocations` — resolvable, unresolvable, and multi-resource/multi-window scenarios: window start/finish, peak demand, tightest capacity, assignment set), extended with the single-kernel segmentation-identity battery (the resource-calendar × availability intersection clipping [11:00, 14:00); the non-working midday break splitting one over-allocation into TWO conflict windows; the two restrictive availability windows splitting the over-allocation at both capacity transitions; and the reordering invariance of BOTH consumers' kernel-driven outputs — allocation tiling AND leveler conflict record — on the segmentation-heavy fixture).
- the critical-path projection `packages/project-renderer-core/src/critical-path.ts` + suite `tests/critical-path.test.ts`: `ProjectCriticalPathSurface`/`ProjectTaskFloat`/`ProjectTaskSlackGeometry` and `buildCriticalPath(document, projection, viewport, rowWindow)` — the VERBATIM echo contract (`critical`/`totalSlack`/`freeSlack` read straight off the projection rows' by-reference `TaskSchedule`; negative slack echoed unclamped), the slack-bar geometry (clamped fractions + edge flags; present iff the authority's totalSlack is positive AND `[scheduledFinish, lateFinish)` is a real span; the snapped-late-date zero-slack case and the inverted negative-slack case carry no geometry), the both-endpoints critical-link classification (document order; one-critical/unscheduled/non-critical endpoints excluded), in-window float scoping, the degenerate-viewport EMPTY surface, rows-without-schedule carrying no float, the timeline integration (present iff the projection joined a schedule; EMPTY values real information; equality with the standalone builder), and 3× determinism + purity.
- the resource projection `packages/project-renderer-core/src/resources.ts` + suite `tests/resource-utilization.test.ts`: the injected `ResourceAllocationQuery` structural type + `ResourceViewInput` (query + the CURRENT derived schedule; per-render input, never persisted state) and `buildResourceUtilization(document, schedule, allocation, window)` — the verbatim-clipped band projection (segments clipped to the viewport, degenerate spans dropped, NO aggregation/merging/classification; demand/capacity/the authority's over-allocation flag/assignment ids echoed verbatim; the name echo present/absent; entries with no in-viewport segments keep EMPTY band lists; the authority's entry order preserved), the degradation contract (degenerate window → ok/empty WITHOUT consulting the evaluator; coded error → `unresolvable` + the diagnostic echoed with the same code; uncoded error re-thrown; non-Error coded throw handled), the evaluator consulted exactly once, and 3× determinism + purity.
- the timeline/Gantt integration `src/views/timeline.ts` + `src/views/gantt-view.ts`: `ProjectTimeline` gains the ADDITIVE optional `criticalPath` (present iff the projection joined a schedule — never invented without one) and `resourceUtilization` (present iff a `ResourceViewInput` was threaded); `buildTimeline`/`buildGanttView` accept the optional `resources` input as the calendar input's sibling; geometry byte-identical with and without either surface (the additive-surface contract, tested).
- the integration suite `tests/critical-resource-integration.test.ts` (the REAL scheduling binding — the no-second-engine evidence): the canonical `resourceAllocations` binding; float EQUALITY with the REAL scheduler's own TaskSchedule values (the chain/branch fixture, the command re-projection, and the MFO impossible schedule); the critical-link classification over the real flags; band EQUALITY with the REAL allocation output clipped to the viewport; the over-allocation windows equaling the REAL leveler's conflict record; the scheduler-first degradation for a broken resource calendar (the schedule itself degrades — the stricter authority — with the pipeline yielding EMPTY surfaces and the diagnostics echoed on the projection); the additive pipeline with BOTH the calendar and the resource inputs threaded (byte-identical geometry); and end-to-end 3× determinism through session commands.
- the golden scenario battery `tests/critical-resource-goldens.test.ts` (R01–R12 over the REAL scheduling package): R01 the critical chain (zero float, no slack bars, chain links classified), R02 the float branch (positive slack echoed with its slack-bar geometry; the branch link excluded), R03 the impossible schedule (negative totalSlack echoed verbatim, never clamped), R04 the zero-slack milestone, R05 the mixed link classification, R06 the calendar-aware demand tiling (equality with the authority), R07 the over-allocation (the band equals the leveler's conflict record), R08 the capacity-window drop (the tightest capacity echoed per segment), R09 the viewport re-projection (clipped bands, unchanged echoes, repeated byte-identically), R10 the command/undo/redo tuple restoration (byte-identical), R11 the absent-input degradation with byte-identical geometry, R12 the 3× byte-identical mixed command/viewport sequence — each golden asserts the complete observable tuple (critical surface, resource surface, ProjectDocument, DerivedSchedule).
- the architecture discipline suite extended: the public-surface lockstep carries the PROJECT-026 exports (`ProjectCriticalPathSurface`, `ProjectTaskFloat`, `ProjectTaskSlackGeometry`, `buildCriticalPath`, `RESOURCE_ALLOCATION_FAILED`, `ProjectResourceBand`, `ProjectResourceUtilization`, `ProjectResourceViewSurface`, `ResourceAllocation`, `ResourceAllocationQuery`, `ResourceAllocationSegment`, `ResourceSurfaceStatus`, `ResourceViewInput`, `buildResourceUtilization`); the timeline/gantt-view source guards (the additive optional surfaces; the `ResourceViewInput` threading); the NEW no-second-CPM-engine guard (scanning `critical-path.ts` for working-time arithmetic and dependency-graph traversal markers — without them no CPM/float computation is possible — plus the verbatim-echo reads) and the NEW no-second-capacity-engine guard (scanning `resources.ts` for the capacity fields, capacity-window data, assignment unit reads, and the calendar evaluators — plus the injected-query-only shape and the echoed over-allocation flag with no renderer-side demand/capacity comparison); the NEW scheduling-side single-authority guards in `packages/project-scheduling/tests/architecture.test.ts` (the kernel module defines the shared allocation semantics incl. the over-allocation predicate; NO other module defines an independent implementation of the kernel primitives — isLeaf/contributesToDemand/effectiveMaxUnits/demandIntervalsForResource/resourceCalendarFor/allocationSegments; the leveler and the allocation projection both import and call the kernel with no surviving sweep markers — boundary collection, working-time evaluation, capacity resolution, the demand rule; no kernel→consumer import cycles) — the guard FAILS if the leveler reintroduces an independent implementation of the shared allocation rules (negative-validated by temporarily re-adding `effectiveMaxUnits` to `leveling.ts`: 2 tests fail, restore → 9 pass); spec lockstep for 026 across requirements/work-items/dependency-graph/verification-matrix; CI lockstep unchanged (the existing foundation gate covers both touched packages and its boundary grep covers them recursively).
- no-regression: all accepted PROJECT-001 through PROJECT-025 tests remain green (contracts 2, engine 205, scheduling 278 → 307 with the kernel/architecture additions, project-file 343, project-renderer-core 362 → 405, project-mpp-host 98) — ZERO skipped; `schedule()`, the command union, the contracts, and the engine are untouched, and `leveling.ts`'s observable PROJECT-013 behavior is byte-identical (the correction extracts its demand/capacity semantics into the shared kernel — every L-golden passes unchanged, zero leveler test modifications).
- formatting: prettier-clean on every file the increment touches; `architecture-lock.md` unchanged (0-line diff).

## PROJECT-027 evidence requirements

PROJECT-027 (Electron shell) must include at minimum:

- the desktop app surface `apps/project` (`@genoffice/project-desktop`): the main process (`src/main/index.ts` + `src/main/menu.ts` + `src/main/bounded-read.ts`) as native transport only — window/lifecycle, the native menu (activation forwarding with UNREGISTERED accelerators), open/save dialogs, size-capped raw byte fs (EVERY read crosses the ONE canonical bounded helper `boundedReadFile` — stat-first rejection, a single stat-size+1 bounded read window, `NativeReadResult` values whose error variant carries no bytes; the cap constant is defined exactly once in the helper), the single-instance/argv open path (the userData path installed BEFORE `requestSingleInstanceLock` — each isolated profile carries its own lock), and the close-guard handshake; NO `@genoffice/*` import anywhere in `src/main`/`src/preload` (asserted by the architecture suite's import scan).
- the typed preload bridge (`src/preload/index.ts`) exposing exactly the shared IPC contract (`src/shared/ipc.ts`) over `contextBridge`; the bridge/IPC lockstep (every `PROJECT_IPC` channel crossed by the preload and served by main) is asserted structurally.
- the renderer binding (`src/renderer/bindings.ts` = the ONE scheduling import site): the three canonical injection seams (`ScheduleRunner` / `CalendarWorkingTimeQuery` / `ResourceAllocationQuery`) wired verbatim; the controller (`src/renderer/app.ts`) with the session/view-state/projection/`buildGanttView` pipeline, the canonical adapter open/save flows, and dirty tracking by document identity; the DOM layer (`src/renderer/ui.ts`) rendering the grid + timeline + editor over the pure view models with `hitTestGantt` as the pointer inverse and keyed in-place grid rows; the translation tables (`src/renderer/translate.ts`) with the menu/keyboard convergence.
- the unit suite (`tests/unit/**`, 77 tests, ZERO skipped, jsdom + the in-memory bridge fake): the translation battery (navigation/extend, editor-mode key ownership incl. the mid-edit Delete/undo pass-through, modifier shortcuts, the menu-id convergence, the complete `MenuCommandId` vocabulary); the host-document battery (the engine-validated/scheduler-derived template, the deterministic template, extension↔format↔adapter mapping, the .gproj round-trip through the REAL adapter incl. the info-diagnostics echo, the MSPDI interchange import, the honest error outcomes); the controller battery (boot chrome; create/select/focus/schedule through the builder; rename/duration edits through the canonical edit flows incl. the real scheduler moving the derived finish and the invalid-draft rejection; undo/redo with the exact-reference restore; dirty-by-identity incl. the undo-to-saved clearing; save/save-as/open/argv-open through the fake bridge with counters; the corrupt-open preservation; the close-guard handshake save/discard/cancel; menu dispatch incl. `fitViewport` equality with the canonical output; keyboard navigation + extend + delete; AND the correction-round read-error outcomes — an oversized picker read and an oversized argv read both surface the transport cap error with the session untouched by reference, and a missing argv path surfaces ENOENT, proving the renderer-side half of "never uncapped contents": a failed read never replaces the session); the canonical bounded-read battery (`tests/unit/bounded-read.test.ts`, direct tests of `src/main/bounded-read.ts` in a node environment: a small file read exactly; an empty file; the exact-boundary read (size === cap → ok); cap+1 rejected on the STAT with NO bytes in the result; the stat-branch proof (a directory whose stat size exceeds a small cap is refused with the SIZE error — an open-then-check implementation would have produced EISDIR instead); ENOENT for a missing path; EISDIR for a directory under the cap; EACCES for a mode-000 file; the default cap pinned to 100 MiB).
- the desktop architecture suite (`tests/unit/architecture.test.ts`, raw-source scans): main/preload package-free; `bindings.ts` the sole scheduling import with exactly the four canonical function imports and the three seams; no working-time/capacity primitive call anywhere in host src; no date computation anywhere in host src; no hand-built `ProjectCommand` literal in the renderer (builders/edit flows only); the menu discipline (registerAccelerator never true; the menu ids = `MenuCommandId` exactly, unique); the preload surface (contextBridge + exactly six invokes); the renderer entry's keyboard path through the translation table; no Electron/Node import in renderer modules; AND the correction-round guards (NEGATIVE-VALIDATED by temporarily reintroducing both reviewed defects: exactly two `boundedReadFile(` call sites — the picker handler and the argv/second-instance handler; NO raw Node `readFile` import or call anywhere in the host main; `MAX_FILE_BYTES` defined only in `bounded-read.ts`; `bounded-read.ts` imports neither electron nor any `@genoffice/*` package (pure Node transport, unit-testable); `app.setPath('userData', …)` appears strictly BEFORE `app.requestSingleInstanceLock()` — the lock is keyed on the userData path).
- the desktop E2E battery (`e2e/project-desktop.spec.ts`, E01–E14, Playwright `electron.launch` of the REAL built app under xvfb — the verification the 021–026 increments deferred to "when a DOM exists to drive"): boot with the native menu vocabulary + displayed accelerators; argv-open rendering the canonical projection end-to-end (rows, the scheduler's own start/finish dates in the grid cells, 3 bars + 1 milestone + 2 links, working AND non-working calendar bands, the critical-chain flags, the float task's 2400-minute slack bar, the resource row + the authority's demand-2/capacity-1 over-allocation band); task creation through the keyboard path + rename through the real cell editor; a duration edit moving the derived finish and the FS successor's start (real scheduling through the session); undo/redo; save-to-path + reopen of the edited state + the on-disk bytes re-imported through the adapter in-test; Save As through the stubbed dialog writing a new canonical file; menu-click open through the REAL application menu + dialog stub → fs read → adapter → renderer; a corrupt open keeping the current document; keyboard navigation/extend + grid click + the `hitTestGantt` bar click; zoom-out widening the axis + fit restoring the span; the close guard (clean immediate close with a Cancel-stubbed dialog proving no consultation, Don't-Save approving, Cancel refusing with the dirty window surviving); dependency link selection + a REAL double-click lag edit through `ChangeLag` (predecessor cell text + the successor's derived start both move); and the two-boot canonical-DOM determinism. The correction-round batteries: **E13 — bounded native reads** (fixtures are VALID documents padded to EXACT byte sizes with JSON trailing whitespace — byte-size is the only variable, so a refused load proves the transport refused, and the transport cap's message is DISTINCT from the adapter's own input-cap message so the E2E asserts WHICH layer refused: an OVERSIZED argv document (cap+1) is refused with the transport error and nothing loads — the renderer never received the bytes; a read at EXACTLY the cap succeeds and delivers exactly the cap through the real bridge → IPC → main-process helper; an OVERSIZED picker selection is refused with the current document surviving; a missing argv path surfaces ENOENT; a directory argv path surfaces EISDIR) and **E14 — single-instance lock isolation** (a second instance sharing the first's scratch dir exits code 0 WITHOUT creating a window and its argv document is forwarded to and loaded by the first instance, which keeps exactly one window; a second instance forwarding an OVERSIZED document surfaces the transport cap error in the FIRST instance; two launches with SEPARATE scratch dirs both stay alive and load their own documents — the discriminating scenario for the lock-ordering correction, since the pre-correction ordering would have keyed both locks on the real profile and made the second launch defer and quit).
- CI: the Project foundation gate grows the typecheck+test steps for `@genoffice/project-desktop` plus the NEW `desktop-e2e` job (ubuntu-22.04, Electron runtime download → `electron-vite build` → AppArmor sysctl best-effort → `xvfb-run` E2E) — the first Project CI stage running the product as an application.
- no-regression: all accepted PROJECT-001 through PROJECT-026 tests remain green (contracts 2, engine 205, scheduling 307, project-file 343 + the spec-consistency guards, renderer-core 405, project-mpp-host 98) — ZERO skipped; no Excel surfaces changed; the foundation packages byte-untouched (the increment adds `apps/project/**` + the workflow + the spec lockstep, nothing else).
- formatting: prettier-clean on every file the increment touches; `architecture-lock.md` unchanged (0-line diff — §3 already sanctions the app surface).
